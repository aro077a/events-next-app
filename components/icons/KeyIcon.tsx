import React from 'react';
import Icon from '@ant-design/icons';
import Key from '@/icons/key.svg';

const KeyIcon = ({...rest}) => {
    return (
            <Icon component={Key} {...rest}/>
    );
};

export default KeyIcon;

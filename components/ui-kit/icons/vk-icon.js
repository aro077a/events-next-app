import React from 'react';
import Icon from '@ant-design/icons';
import Vk from '@/styles/icons/vk.svg';

const VkIcon = ({...rest}) => {
    return (
            <Icon component={Vk} {...rest}/>
    );
};

export default VkIcon;
